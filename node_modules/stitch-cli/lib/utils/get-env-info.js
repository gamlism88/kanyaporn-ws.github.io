"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function default_1(config, command, registryUrl, authorizationHeader = '') {
    const printableAuthHeader = authorizationHeader
        .split(' ')
        .map(w => w
        .split('')
        .map((ch, idx) => (idx > 3 && idx < w.length - 3 ? '*' : ch))
        .join(''))
        .join(' ');
    return `
    Name:           ${config.name}
    Version:        ${config.version}
    Command:        ${command}
    Root directory: ${config.root}
    User agent:     ${config.userAgent}
    Platform:       ${config.platform}
    Architecture:   ${config.arch}
    Shell:          ${config.shell}
    CWD:            ${process.cwd()}

    RegistryUrl:    ${registryUrl}
    Authorization:  ${printableAuthHeader}

  `;
}
exports.default = default_1;
