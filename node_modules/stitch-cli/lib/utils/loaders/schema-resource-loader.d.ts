import { ResourceMetadataInput, SchemaInput } from '../../client';
interface SchemaResourceInput {
    metadata: ResourceMetadataInput;
    schema?: string;
    schemaFiles?: string[];
}
export default function (schemaResource: SchemaResourceInput, resourceFile: string): Promise<SchemaInput>;
export {};
