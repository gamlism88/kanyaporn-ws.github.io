"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = require("path");
async function default_1(path, extensions = ['yaml', 'yml']) {
    const fileList = [];
    await walk(fileList, path, extensions);
    return fileList;
}
exports.default = default_1;
async function walk(fileList, path, extensions = ['yaml', 'yml']) {
    const stat = await fs_1.promises.stat(path);
    if (stat.isDirectory()) {
        const files = await fs_1.promises.readdir(path);
        for (const file of files) {
            await walk(fileList, path_1.join(path, file), extensions);
        }
        return;
    }
    const isResource = extensions.some(ext => path.endsWith(`.${ext}`));
    if (isResource) {
        fileList.push(path);
    }
}
