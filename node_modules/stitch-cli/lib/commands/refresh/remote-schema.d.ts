import { Command, flags } from '@oclif/command';
export default class ApplyResources extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        'registry-url': flags.IOptionFlag<string>;
        'authorization-header': flags.IOptionFlag<string | undefined>;
        timeout: import("@oclif/parser/lib/flags").IOptionFlag<number>;
        verbose: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    static args: {
        name: string;
        required: boolean;
    }[];
    private verboseMode;
    run(): Promise<void>;
    trace(message?: string, ...args: any[]): Promise<void>;
}
